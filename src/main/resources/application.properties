spring.application.name=demo

# Conexión Dinámica a la Base de Datos (Usa variables de entorno, si no existen usa los valores por defecto locales)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/oficina}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:13327031Sebas}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración del Servidor
server.forward-headers-strategy=framework
server.port=${SERVER_PORT:8080}

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect